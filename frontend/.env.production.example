# Production Environment Variables for Frontend
# Copy this file to .env.production and fill in your values

# Backend API URL (production)
# This should be your production API domain
# For deployment at www.bahar.co.il/mytrips-viewer/
REACT_APP_BACKEND_URL=https://www.bahar.co.il/mytrips-viewer-api

# Google Maps API Key (production)
# IMPORTANT: Use a key restricted to your production domain
# Configure restrictions at: https://console.cloud.google.com/apis/credentials
REACT_APP_GOOGLE_MAPS_API_KEY=your-production-google-maps-api-key-here

# Location API Base URL (production)
# All location API calls go through this endpoint
REACT_APP_LOC_API_BASEURL=https://mytrips-api.bahar.co.il/location/api

# Location API Token (production)
# Contact your Location API administrator for the token
REACT_APP_LOC_API_TOKEN=your-location-api-token-here

# MyTrips API (for authentication)
REACT_APP_MYTRIPS_API_BASEURL=https://mytrips-api.bahar.co.il

# Notes:
# - These values are embedded in the build at build time
# - You must rebuild the frontend if you change these values
# - Never commit .env.production to version control
# - Use domain restrictions on your Google Maps API key
# - Keep LOC_API_TOKEN secure - never expose in frontend code

