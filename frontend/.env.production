# Frontend Production Environment Variables
# These values are embedded into the build at build time
#
# SECURITY: Do NOT commit actual secrets to this file!
# Use environment variables or CI/CD secrets management instead.
# See .env.production.example for the template.

# Backend API URL (production)
# SECURITY FIX: Backend now proxies Location API calls to keep LOC_API_TOKEN server-side
# The backend must be deployed at this URL for the frontend to work
REACT_APP_BACKEND_URL=https://www.bahar.co.il/mytrips-viewer-api

# Google Maps API Key (production)
# IMPORTANT: Restrict this key to www.bahar.co.il in Google Cloud Console
# SECURITY: Use environment variable or CI/CD secrets, do NOT hardcode
REACT_APP_GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# SECURITY NOTE: LOC_API_TOKEN and LOC_API_BASEURL are NO LONGER in frontend
# These are now managed server-side in the backend for security
# The backend proxies all Location API calls to prevent token exposure

# MyTrips API (for authentication)
REACT_APP_MYTRIPS_API_BASEURL=https://mytrips-api.bahar.co.il